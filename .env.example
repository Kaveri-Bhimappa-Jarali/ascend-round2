DEMO_MODE=true
DATABASE_URL=sqlite:///./data/cloud_compliance.db
SCAN_INTERVAL_SECONDS=30

# AWS and GCP use their official SDK credential mechanisms.
# Do not put secrets in source control.
# AWS_PROFILE=default
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
